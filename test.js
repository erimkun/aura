import fs from 'fs'; import { GoogleGenAI } from '@google/genai'; const ai = new GoogleGenAI({apiKey: process.env.GEMINI_API_KEY}); async function run() { console.log('testing AI with gemini-3-flash-preview for text...'); const r = await ai.models.generateContent({model: 'gemini-3-flash-preview', contents: 'hello'}); console.log('Text result:', r.text); } run().catch(console.error);
